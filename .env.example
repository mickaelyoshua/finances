# Local development (docker-compose)
DATABASE_URL=postgres://finances:finances@localhost:5432/finances

# Production (Neon) — uncomment and fill in your credentials
# DATABASE_URL=postgresql://<user>:<password>@<host>.neon.tech/finances?sslmode=require
